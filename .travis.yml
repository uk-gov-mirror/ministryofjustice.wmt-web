before_install:
  - curl -o- -L https://yarnpkg.com/install.sh | bash -s -- --version v0.27.5
  - export PATH="$HOME/.yarn/bin:$PATH"

language: node_js
node_js:
- '6'
cache: yarn
script: yarn unit-test